# Flat Namespace with Delimiters

The storage system will use a flat namespace where "folders" are simulated using delimiters in the file keys (e.g., `path/to/file`). This avoids the complexity of managing actual directory structures while providing the same organizational utility.